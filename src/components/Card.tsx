import React from 'react'

interface CardProps {
    icon: string;
    title: string;
    content: string;
    index: number;
}

const Card: React.FC<CardProps> = () => (
    <div className=
        {`
        `}
    >
    </div>
)

export default Card
