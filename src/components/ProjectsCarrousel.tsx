import styles from '../style'
import { layout } from '../style'


function ProjectsCarrousel() {
  return (
    <section id="projects-carrousel"
      className=
      {`
        ${layout.section}
      `}
    >
      
    </section>
  )
}

export default ProjectsCarrousel
