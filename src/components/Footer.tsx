import styles from "../style"

function Footer() {
  return (
    <section className=
      {`
        ${styles.flexCenter}
        ${styles.paddingY}
        flex-col
      `}
    >

    </section>
  )
}

export default Footer
